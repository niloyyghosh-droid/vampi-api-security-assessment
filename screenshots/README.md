# Evidence Screenshots

The original assessment package contains two PNG proof-of-concept screenshots:

- `api-vuln01-single-user.png` — unauthenticated single-user lookup
- `api-vuln01-full-dump.png` — unauthenticated full user-list exposure

The connected GitHub integration can create text files directly, but it does not expose a binary file upload action for the supplied PNG files.